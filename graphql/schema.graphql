type AppSettings {
  account: String!
  vtexAppKey: String!
  vtexAppToken: String!
}

type Category {
  id: String!
  name: String!
  subCategories: [Category!]
}

type Query {
  categories: [Category!]
  appSettings: AppSettings!
}
